<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esielkar.calificame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Califícame">
        <activity android:name=".view.SubjectActivity"></activity>
        <activity android:name=".view.FacultyActivity" />
        <activity android:name=".view.UniversityActivity" />
        <activity android:name=".view.PasswordForgottenActivity" />
        <activity android:name=".view.AboutUsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".view.SignUpActivity" />
        <activity android:name=".view.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>